[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0.0"
flask-login = ">=0.6.3"
flask-wtf = ">=1.2.1"
flask-sqlalchemy = ">=3.1.1"
wtforms = ">=3.1.2"
email-validator = ">=2.0.0"
requests = ">=2.28.0"

[dev-packages]

[requires]
python_version = "3.8"
